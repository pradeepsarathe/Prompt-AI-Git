# robots.txt — promptai.in
# Open access policy: every well-behaved crawler is welcome.
# This includes both search-engine crawlers (Google, Bing, etc.) AND
# AI crawlers (GPTBot, ClaudeBot, PerplexityBot, CCBot, Google-Extended, …).
# We want our content discovered, indexed, and cited everywhere.

User-agent: *
Allow: /

# Sitemap location (absolute URL, per spec)
Sitemap: https://promptai.in/sitemap.xml
