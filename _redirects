# Cloudflare Pages redirects
#
# NOTE: /feed.xml and /sitemap.xml are served DIRECTLY by Pages Functions
# (functions/feed.xml.js and functions/sitemap.xml.js). Do NOT re-add
# `200` proxy rewrites for them — Cloudflare only resolves 200 rewrites to
# static assets, never to Functions, so those rules 404. See the header
# comments in those function files.

# www → apex redirect
https://www.promptai.in/* https://promptai.in/:splat 301
